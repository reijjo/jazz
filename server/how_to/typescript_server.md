# TypeScript backend

## Init folder & add .gitignore

- `mkdir server` `cd server` `npm init -y`
- `touch .gitignore`

```
node_modules
.DS_Store
```

## Init TypeScript

- `npm install typescript --save-dev`
-
