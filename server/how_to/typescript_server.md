# TypeScript backend

## Init folder & add .gitignore

- `mkdir server` `cd server` `npm init -y`
- `touch .gitignore`

```
node_modules
.DS_Store
```

## Init TypeScript

- `npm install typescript --save-dev`
- add to package.json

```.json
  "scripts": {
    "tsc": "tsc"
  },
```
